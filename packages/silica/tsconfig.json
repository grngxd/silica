{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "rootDir": "../..", // Set to the root of the monorepo
        "outDir": "out"
    },
    "include": ["../../packages/**/*"], // Include all packages
    "exclude": ["node_modules", "out"]
}